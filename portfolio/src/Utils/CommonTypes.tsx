export type Link = {
    link: string,
    alias: string
};

/* Use it when you need to specify the way or direction of showing items in container or whatever */
export enum Direction {
    rtl,
    ltr
};